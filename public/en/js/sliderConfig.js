(function(){
    // Slider

    revapi = jQuery('.tp-banner').revolution(
     {
         delay:9000,
         startwidth:1170,
         startheight:400,
         hideThumbs:10,
         fullWidth:"on",
         spinner: "spinner2",
         forceFullWidth:"on",
         navigationStyle: "preview1"

     });

})();
